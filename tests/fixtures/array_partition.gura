fn swap(a: mut [i64], i: i64, j: i64): unit {
  let saved: i64 = a[i]
  a[i] = a[j]
  a[j] = saved
}

fn partition(a: mut [i64], lo: i64, hi: i64): i64 {
  let pivot: i64 = a[hi]
  var i: i64 = lo - 1
  var j: i64 = lo

  while j < hi {
    if a[j] <= pivot {
      i = i + 1
      swap(a, i, j)
    }
    j = j + 1
  }

  swap(a, i + 1, hi)
  return i + 1
}

fn main(): i64 {
  let a: mut [i64] = [5, 2, 9, 1, 3]
  let p: i64 = partition(a, 0, 4)
  return (p - 2) + (a[0] - 2) + (a[1] - 1) + (a[2] - 3) + (a[3] - 5) + (a[4] - 9)
}
