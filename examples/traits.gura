struct Point {
    let x: i64
}

trait Named {
    fn name(self: imm): i64
}

impl Named for Point {
    fn name(self: imm): i64 {
        return self.x
    }
}

fn main(): i64 {
    var p: imm Point = new imm Point { x: 1 }
    return p.name()
}
